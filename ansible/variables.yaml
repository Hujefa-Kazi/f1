- name: this is variable learning
  hosts: all 
  #vars:
  #animal: "tiger"
  #vars_files:
  #- ./var.txt  
  #vars_prompt:
  #prompt: enter your animal name
     
  tasks:
    #- name: print animal 1
    #  debug:
    #   msg: "{{animal}}"
    #- name: print animal 2
    #  debug:
    #    msg: "{{animal}}"
    #- name: print aniaml 3
    #  debug:
    #    msg: "{{animal}}"
    #  vars: 
    #    animal: "Lion"

    - name: get hostname
      shell: hostname
      register: machineName

    - name: print hostname
      debug:
        msg: "{{machineName}}"